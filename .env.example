# ─── Admin Key (for the admin panel) ───
ADMIN_KEY=change-me-to-a-secret-key

# ─── SMTP / Email (SendGrid — free 100 emails/day) ───
# Sign up at https://sendgrid.com → Settings → API Keys → Create Key
# Then: Settings → Sender Authentication → verify your sender email
# Set these same variables in Netlify → Site configuration → Environment variables
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASS=SG.your-sendgrid-api-key-here

# ─── Server (local dev only, ignored on Netlify) ───
PORT=3000
